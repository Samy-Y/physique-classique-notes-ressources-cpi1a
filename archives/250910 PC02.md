# Cinématique classique du point

## Notions de base
* La cinématique consiste à décrire une trajectoire, un mouvement, sans se préocuper la source de ce changement (dont on se charge en étude dynamique).
* On doit préciser le référentiel, l'observateur est très important.
* Un référentiel est "composé" de deux repères:
  * Un repère d'espace
  * Un repère de temps
* Préciser la nature d'une trajectoire n'a de sens que si l'on précise l'observateur.
* L'observateur est fixe dans le repère d'espace.
* En mécanique classique, l'espace est "homogène", de courbure nulle — un espace plat. On parle de géométrie euclidienne (ou d'un espace euclidien).
  * Le plan → courbure nulle $\not ={}$ Sphère
  * Les distances sont définies de différentes manières.

<img src="./assets/250910 pc02 01.png" height=300>

* Les distances sont des grandeurs absolues en géométrie euclidienne (et non pas relatives, elles ne dépendent pas de l'observateur).
* On admet que le temps est absolu, et qu'il ne dépend pas de l'observateur.
  * En revanche, lorsqu'on observe des *phénomènes relativistes* (courbure forte de l'espace temps, vitesses proches de celle de la lumière), on ne peut plus considérer que le temps est absolu et qu'il s'écoule de la même manière pour tous les observateurs.
---
## Système de coordonnées cartésiennes
* Lors de la création d'un repère dans l'espace, il suffit que les trois directions "essentielles" soient linéairement indépendantes.
* Dans un système de coordonnées cartésiennes, les trois directions/axes (bases) sont orthogonales, concourrent en O et sont normées, c.à.d. : $||\vec{e_x}|| = ||\vec{e_y}|| = ||\vec{e_z}|| = 1$. Les bases sont directes.
* Soit $M$ un point de l'espace de coordonnées $(x,y,z)$. On peut dire que: $\vec{OM}.\vec{e_x} = x$, $\vec{OM}.\vec{e_y} = y$ et $\vec{OM}.\vec{e_z} = z$
* On exprime la trajectoire à travers des fonctions dont le paramètre est le temps.

---
## Coordonnées cylindriques
* Certains mouvements (circulaires, hélicoïdaux...) ont un axe priviligié (une sorte d'axe de symétrie du mouvement), et le système cartésien devient inadapté (mais utilisable), on va utiliser à la place des coordonnées cylindriques.
* On passe de $M(x,y,z)$ à $M(r,\theta,z)$ (si on admet que $z$ est la direction priviligée lors de l'étude).
* On doit choisir une direction de référence à partir du repère cartésien pour pouvoir calculer $\theta$.
* On définit une nouvelle base directe de coordonnées cylindriques à partir des bases cartésiennes déjà connues en faisant simplement quelques projections :
  * $\vec{e_\theta} = -\sin{\theta}.\vec{e_x} + \cos{\theta}.\vec{e_y}$
  * $\vec{e_r} = \cos{\theta}.\vec{e_x} + \sin{\theta}.\vec{e_y}$
* Lorsqu'on essaie de définir $\vec{OM}$, on remarque que : $\vec{OM} = x.\vec{e_x} + y.\vec{e_y} + z.\vec{e_z} = r.\vec{e_r} + z.\vec{e_z}$. On se demande légitemement où est le "paramètre" $\theta$ nécessaire à la construction d'un système de coordonées dans l'espace. Il est en réalité un paramètre de $\vec{e_r}$.
* On peut donc dire que $(\vec{e_r},\vec{e_\theta})$ est dépendant de $\theta$, donc de la position de $M$, et est ainsi une **base mobile**.
* Il est nécessaire de remarquer que $(\vec{e_x},\vec{e_y},\vec{e_z})$ sont indépendants de cette nouvelle base.

<div style="background:#eee; border:1px solid #ccc; max-width: fit-content; padding:5px;color:#333;">250911</div>

* $\vec{e_r} = \frac{\vec{OP}}{r} = x.\vec{e_x} + y.\vec{e_y} = \sqrt{x²+y²}$

* $(_{\vec{e_r}}^{\vec{e_\theta}}) =
\begin{pmatrix}{\cos{\theta}} & {\sin{\theta}} \\ -\sin{\theta} & {\cos{\theta}}\end{pmatrix}(^{\vec{e_x}}_{\vec{e_y}})$

* Redémontrer les propriétés et calculs des bases dans les coordonnées sphériques.
* On parle de vecteur à norme constante quand $||\vec{v}|| = \sqrt{\vec{v}.\vec{v}} = cte.$
* On peut étudier les variations élémentaires de ce vecteur à travers $d\vec{v}$. On peut dériver $||\vec{v}||² = \vec{v}.\vec{v}$ pour obtenir $2\vec{v}.d\vec{v}$, qui doit être nul. 
* On en déduit que $d\vec{v} \perp \vec{v}$.
* Les fonctions vectorielles n'ont rien d'hors du commun et peuvent être étudiées aussi simplement que les fonctions réelles familières.

    * **Fonctions réelles**
        * $f(x)$ (valeur réelle)
        * $f'(x) = \frac{df(x)}{dx}$ (dérivée)
        * $df(x)$ (différentielle ou variation élémentaire)

    * **Fonctions vectorielles (cas général)**
        * $\vec{V}(t)$ (vecteur, souvent dépendant du temps $t$)
        * $\frac{d\vec{V}(t)}{dt}$ (vecteur dérivée)
        * $d\vec{V}$ (vecteur variation élémentaire ou différentielle)

---

## Cas d'un Vecteur à Norme Constante

💡 Un vecteur peut changer tout en gardant sa "longueur" (sa norme) constante. Pensez à l'aiguille d'une horloge : sa longueur est fixe, mais sa pointe est constamment en mouvement, donc sa direction change.

Si la norme de $\vec{V}(t)$, notée $\|\vec{V}(t)\|$, est une constante $C$, alors :
$$ \|\vec{V}(t)\| = C \quad \implies \quad \|\vec{V}(t)\|^2 = C^2 $$

Or, le carré de la norme est le produit scalaire du vecteur avec lui-même :
$$ \vec{V} \cdot \vec{V} = C^2 $$

Maintenant, dérivons cette expression par rapport au temps $t$. La dérivée d'une constante ($C^2$) est zéro. Pour le membre de gauche, on utilise la formule de dérivation d'un produit scalaire :
$$ \frac{d}{dt}(\vec{A} \cdot \vec{B}) = \frac{d\vec{A}}{dt} \cdot \vec{B} + \vec{A} \cdot \frac{d\vec{B}}{dt} $$

En appliquant cela à $\vec{V} \cdot \vec{V}$ :
$$ \frac{d}{dt}(\vec{V} \cdot \vec{V}) = \frac{d\vec{V}}{dt} \cdot \vec{V} + \vec{V} \cdot \frac{d\vec{V}}{dt} = 2 \vec{V} \cdot \frac{d\vec{V}}{dt} $$

Puisque $\frac{d}{dt}(C^2) = 0$, nous obtenons la relation fondamentale :
$$ 2 \vec{V} \cdot \frac{d\vec{V}}{dt} = 0 \quad \implies \quad \vec{V} \cdot \frac{d\vec{V}}{dt} = 0 $$

### Conclusion et Interprétation

Le produit scalaire de deux vecteurs non nuls est nul si et seulement si ces deux vecteurs sont **orthogonaux** (perpendiculaires).

➡️ **Propriété :** Pour un vecteur $\vec{V}$ de norme constante, son vecteur dérivée $\frac{d\vec{V}}{dt}$ lui est toujours orthogonal.



#### Et pour la variation élémentaire $d\vec{V}$ ?

La relation est identique. Comme $d\vec{V} = \frac{d\vec{V}}{dt} dt$, le vecteur variation élémentaire $d\vec{V}$ a la même direction que le vecteur dérivée. Par conséquent :

$$ \vec{V} \cdot d\vec{V} = 0 $$

➡️ La variation élémentaire $d\vec{V}$ d'un vecteur $\vec{V}$ à norme constante est toujours orthogonale à $\vec{V}$.

🌍 **Exemple physique :** Le mouvement circulaire uniforme.
* Le **vecteur position** $\vec{r}$ (du centre vers l'objet) a une norme constante (le rayon).
* Le **vecteur vitesse** $\vec{v} = \frac{d\vec{r}}{dt}$ est la dérivée du vecteur position.
* Conformément à notre résultat, la vitesse $\vec{v}$ est toujours perpendiculaire à la position $\vec{r}$ : elle est tangente à la trajectoire circulaire.
---
* On peut écrire les fonctions vectorielles sous cette forme :
$$\vec{F}(x,y,z)$$
$$= F_x(x,y,z).\vec{e_x} + F_y(x,y,z).\vec{e_y} + F_z(x,y,z).\vec{e_z}$$
* On peut étudier les variations élémentaires des autres fonctions vectorielles qui composent cette grande fonction de la manière suivante :
$$d\vec{F}(x,y,z) = dF_x(x,y,z)\vec{e_x} + dF_y(x,y,z)\vec{e_y} + dF_z(x,y,z)\vec{e_z}$$
* Et on a : $dF_x(x,y,z) = F_x(x+dx,y+dy,z+dz) - F_x(x,y,z)$
* On peut étudier les variations élémentaires de la position d'un point M afin d'étudier sa vitesse, vu que la vitesse n'est que la dérivée de la position par rapport au temps. Prenons par exemple un point M de l'espace, localisé avec un repère de coordonnées cartesiennes. On pourra exprimer $d\vec{OM}$ en utilisant tout simplement l'équation de $\vec{OM}$.
  * On a : $\vec{OM}(t) = x(t).\vec{e_z} + y(t).\vec{e_y} + z(t).\vec{e_z}$
  * Et on a: $d\vec{OM} = \vec{OM}(t +dt) - \vec{OM}(t)$
  * Après quelques calculs triviaux, on trouve:
$$d\vec{OM}=dx.\vec{e_x}+dy.\vec{e_y}+dz.\vec{e_z}$$
* On peut réaliser la même opération en coordonnées cylindriques :
  * On a: $\vec{OM}(t)=r(t).\vec{e_r}(\theta(t))+z(t).\vec{e_z}$ (on n'oublie pas que $\vec{e_r}$ dépend de $\theta(t)$).

* En ce qui concerne les coordonnées sphériques, on suit la même logique.
<div style="background:#eee;border:1px solid #ccc; text-align:center; font-weight:bold;">⚠️ REFAIRE LES DEMONSTRATIONS, SURTOUT EN COORDONNEES SPHERIQUES ⚠️</div>

<div style="background: #fafafaff ;border:1px solid #eee; text-align:center; padding:10px; border-radius:10px; margin-top:10px;">✅ Démonstrations refaites le 14/09/2025</div>
